✅ Git Tutorial (Bangla + Practical Work)
🔹 1. কেন (Why Git ব্যবহার করবো?)

Git vs GitHub

Git → Version control system (লোকাল/PC তে কাজ হয়)।

GitHub → Git এর Remote Repo (Server), যেখানে কোড share/backup রাখা হয়।

কেন দরকার?

Project এর version control রাখার জন্য (কে কবে কোন কোড লিখেছে / পরিবর্তন করেছে)।

Team এ কাজ করলে collaboration করার জন্য।

ভুল হলে আগের version এ ফিরে যাওয়ার জন্য।

Professional কাজের ক্ষেত্রে code history প্রমাণ হিসাবে দরকার হয়।

🔹 2. কখন (When Git Operation করবো?)

লোকাল মেশিনে প্রোজেক্ট তৈরি/এডিট করার সময় Git ব্যবহার করতে হবে।

Check hidden files:

ls
ls -a

🔹 3. কোথায় (Where Git Use করবো?)

Local Machine (নিজের PC) → Development (code লেখা, edit করা)।

Remote Repo (GitHub, GitLab, Bitbucket) → Backup + Team Share।

Production/Server → Deploy করার আগে latest code pull করতে হবে।

Commit করতে হবে (✔️)

/routes → API routes

/controllers → Business logic

/services → DB connection, Cloud service

/views → Templates (EJS/Pug/HBS)

server.js → Entry point

package.json → Dependency list

Commit করবে না (❌)

.env (Sensitive data – DB, API Key)

node_modules/ (Dependency folder – অনেক বড় হয়)

package-lock.json (Conflict হয়, team policy উপর depend করে)

.gitignore file তৈরি করতে হবে:

# dependencies
node_modules/

# environment variables
.env

# lock file
package-lock.json

🔹 Local থেকে Remote (Local → Remote)

1️⃣ Git Init

git init


👉 Criminal ফোল্ডারে .git নামে hidden folder তৈরি হবে।

ভুলবশত .git থাকলে মুছে দাও:

rm -rf .git


2️⃣ Remote Repo Connect

git remote add origin https://github.com/username/CriminalBackendProject.git


3️⃣ Stage Files

git add .


4️⃣ Commit Files

git commit -m "Initial commit"


5️⃣ Push to GitHub

git branch -M main
git push origin main

🔹 Remote থেকে Local (Remote → Local)

1️⃣ Clone Repo

git clone https://github.com/username/CriminalBackendProject.git


2️⃣ Pull Changes

git pull origin main

🔹 Important Git Commands (with Why)
Command	কাজ (বাংলা)	Why (কেন দরকার)
git init	Criminal প্রোজেক্টে Git চালু	ফাইল ট্র্যাক করার জন্য
git clone	Remote Criminal repo লোকালে নামানো	অন্য PC/টিমে প্রোজেক্ট পেতে
git add .	Criminal এর সব ফাইল stage করা	কোন ফাইল commit হবে তা নির্ধারণ করতে
git commit -m "msg"	Local checkpoint বানানো	Code history save করতে
git push origin main	Local Criminal → Remote পাঠানো	টিম/সার্ভারের সাথে share করতে
git pull origin main	Remote → Local sync করা	টিমমেটের update লোকালে পেতে
git branch	Criminal এর branch লিস্ট দেখা	একাধিক feature manage করতে
git checkout -b new-branch	নতুন branch তৈরি করা	Main code safe রেখে feature বানাতে
git merge branchname	Branch main এ merge করা	Feature শেষ হলে মূল কোডে যোগ করতে
git status	কোন ফাইল পরিবর্তিত দেখানো	কোন কাজ commit হয়নি বুঝতে
git log	Commit history দেখা	কে কখন কী করেছে track করতে
git stash	Temporary save	অর্ধেক কাজ রেখে অন্য কাজ করতে
🔹 Pull Request (PR)

Definition → তোমার কোডের পরিবর্তন main/master branch এ merge করার জন্য অনুরোধ।

Types:

Same Repo PR (Internal) → feature branch → main merge

Forked Repo PR (External) → নিজের fork → main repo তে PR

🔹 File Tracking System

Untracked File → Git এখনো track করছে না

git add filename


Tracked (Staged/Committed) → add করার পর track হবে

Modified File → Edit করলে আবার stage করতে হবে

🔹 Merge Conflict

Example:

<<<<<<< HEAD
const name = "Hasan";
=======
const name = "Karim";
>>>>>>> feature


✅ Fix → কোনটা রাখবে সেটা manually ঠিক করতে হবে।

const name = "Karim";

🔹 Undo / Rollback

Last commit undo (code safe থাকবে):

git reset --soft HEAD~1


Last commit + code remove:

git reset --hard HEAD~1

🔹 Commit History দেখার জন্য
git log
git log --oneline

🎯 Final Flow (Summary)
Local → Remote

git add . → git commit -m "msg" → git push origin main

Remote → Local

git clone → git pull origin main