🔹 1. কেন (Why Git ব্যবহার করবো?)

আমাদের প্রোজেক্টের version control রাখার জন্য (কে কবে কোন কোড লিখেছে / পরিবর্তন করেছে)।

Team এ কাজ করলে collaboration করার জন্য।

ভুল হলে আগের version এ ফিরে যাওয়ার জন্য।

Professional কাজের ক্ষেত্রে code history প্রমাণ হিসাবে দরকার হয়।

🔹 2. কখন (When Git Operation করবো?)

| কাজ          | কখন করতে হবে                                             |
| ------------ | -------------------------------------------------------- |
| `git init`   | একদম নতুন project তৈরি করার সময়                          |
| `git clone`  | অন্যের project নিজের কম্পিউটারে নামানোর সময়              |
| `git add`    | ফাইল edit বা নতুন ফাইল create করার পর                    |
| `git commit` | কাজ সেভ/lock করার সময় (code checkpoint) (git commit -m "message") |
| `git push`   | local কাজ server (GitHub/GitLab) এ পাঠানোর সময় (git push origin branch)          |
| `git pull`   | টিমমেট নতুন কাজ করলে সেটা নিজের সাথে sync করার সময় (git pull origin branch)      |
| `git branch` | নতুন feature/bug-fix এর জন্য আলাদা লাইন তৈরি করার সময়     |
| `git merge`  | আলাদা branch এর কাজ মূল / main branch এ মেশানোর সময়   |
| `git status` | কোন ফাইল পরিবর্তন হয়েছে দেখতে                            |
| `git log`    | পুরো history দেখতে                                       |
| `git revert` | ভুল commit undo করতে                                     |
| `git stash`  | কাজ অসম্পূর্ণ রেখে অন্য কিছু করতে চাইলে সাময়িক save করতে |

🔹 3. কোথায় (Where Git Use করবো?)

Local Machine (নিজের কম্পিউটার) → Development চলবে (code লেখা, edit করা)।

Remote Repository (যেমন GitHub, GitLab, Bitbucket) → Server এ কোড রাখা হবে (backup + team share)।

লোকাল মেশিনে: তুমি কাজ করবে, commit করবে।

Remote repo (GitHub/GitLab): Team এর সবাই share করবে।

Production/Server deploy করার আগে: latest code pull করতে হবে।

✅ Git এ commit করতে হবে (Important Files)

/routes → (API routes রাখবে)

/controllers → (business logic)

/services → (DB connection, Cloudinary service)

/views → (EJS / templates)

server.js → (entry point of project)

package.json → (সব dependency list থাকবে – অন্য developer install করতে পারবে npm install দিয়ে)

❌ Git এ commit করবে না (Avoid)

.gitignore ফাইল ব্যবহার করা হয় যাতে কিছু ফাইল বা ফোল্ডার Git repository-তে ট্র্যাক না হয় (commit/push এ না যায় .env, node_modules/, package-lock.json)

👉 On GitHub repo creation screen, if you tick Add .gitignore(Node), you can choose a template (like Node, Python, Java, etc.).
Visibility: Private (if personal) / Public (if sharing)

Add README: ✔️ Yes

Add .gitignore: ✔️ Yes → Choose project type (Node/Python/etc.) no need VS code .gitignore file

Add license: ✔️ Yes (MIT is simplest)

node_modules/

এখানে সব dependency ফাইল থাকে।

খুব বড় সাইজের হয়, তাই Git এ রাখলে repo ভারী হয়ে যায়।

এর দরকার হয় না, কারণ অন্য কেউ project clone করলে → শুধু npm install চালালেই package.json থেকে আবার install হয়ে যাবে।

.env

এখানে sensitive data থাকে (DB password, API key, Cloudinary secret)।

Public repo তে গেলে হ্যাকার ব্যবহার করতে পারবে।

তাই .gitignore ফাইলে .env যোগ করতে হবে।

package-lock.json

এই ফাইল auto generate হয় যখন npm install করি।

Local এ dependency lock করে রাখে।

Team এ কাজ করলে conflict হয়।

Professional workflow এ team যদি একই dependency version maintain করে 
তবে এটাও Git ignore করা হয় (কিছু কোম্পানি commit করে, কিছু ignore করে – depend করে policy এর উপর)।


🔹 Local থেকে Remote (Local → Remote)
1. git init → Criminal প্রোজেক্টে Git চালু

Why? 👉 Criminal ফোল্ডারে .git নামে hidden ফোল্ডার বানায়। এই .git ফোল্ডারেই তোমার প্রোজেক্টের সব পরিবর্তনের ইতিহাস (history, commit, branch, merge ইত্যাদি) সেভ থাকে।
👉 .git ছাড়া Git তোমার প্রোজেক্ট ট্র্যাক করতে পারবে না।

যদি ভুলবশত আগে থেকেই .git থাকে:

cd H:/workspace2/Criminal/
rm -rf .git


👉 পুরোনো history মুছে দিয়ে fresh করে নতুন Git শুরু করা যাবে।

2. Remote Repo Connect
git remote add origin https://github.com/Papayd60/CriminalBackendProject.git


Why? 👉 লোকাল Criminal প্রোজেক্টকে GitHub এর সাথে লিঙ্ক করতে হয়।
🔹 Local = তোমার PC
🔹 Remote = GitHub server
👉 connect করলে তুমি local → remote (push) আর remote → local (pull) করতে পারবে।

3. .gitignore বানানো
# dependencies
node_modules/

# environment variables
.env

# lock file
package-lock.json


Why? 👉 কিছু জিনিস GitHub এ কখনো আপলোড করা যাবে না:

node_modules/ → অনেক বড় ফোল্ডার, লাইব্রেরিগুলো আলাদাভাবে npm install দিয়ে নামানো যাবে।

.env → গোপন API key, password, DB config leak হয়ে যাবে।

package-lock.json → সিস্টেমভেদে conflict হয়, দরকার নেই টিমে।

4. Stage Changes
git add .


Why? 👉 Git কে বলা হয় — "এই Criminal প্রোজেক্টের ফাইলগুলো আমি next commit করতে চাই"।
👉 git add . মানে Criminal প্রোজেক্টের সব ফাইল stage হলো।

5. Commit Changes
git commit -m "Initial commit"


Why? 👉 এটা হলো প্রোজেক্টের checkpoint/save point।
👉 ভবিষ্যতে কোনো ভুল হলে এই commit থেকে project rollback করতে পারবে।

6. Push to GitHub
git branch -M main   # যদি branch নাম master হয় তবে main করে দাও
git push origin main


Why? 👉 Criminal প্রোজেক্টের local history GitHub server এ আপলোড হয়ে যাবে।
👉 টিমের সবাই এখন প্রোজেক্ট দেখতে/ডাউনলোড করতে পারবে।



🔹 Remote থেকে Local (Remote → Local)

👉 ধরো তুমি নতুন মেশিনে কাজ করবে বা টিমমেট তোমার প্রোজেক্ট চাইবে।

Clone করা

git clone https://github.com/Papayd60/CriminalBackendProject.git


Why? 👉 Remote Criminal repo কে নামিয়ে লোকাল কম্পিউটারে আনতে।

Remote এর পরিবর্তন আনতে

git pull origin main


Why? 👉 টিমমেট যদি Criminal প্রোজেক্টে কিছু নতুন কোড যোগ করে → সেটা তোমার লোকালেও আপডেট হবে।

🔹 প্রতিটি Command এর Why (বাংলায়)
Command	কাজ (বাংলা ব্যাখ্যা)	Why (কেন দরকার)
git init	লোকাল Criminal প্রোজেক্টে প্রথমবার Git চালু করতে	Git ফাইল ট্র্যাক করা শুরু করতে
git clone	Remote Criminal repo লোকালে নামাতে	টিম/অন্য মেশিনে প্রোজেক্ট পেতে
git add .	Criminal এর সব ফাইল stage করতে	কোন ফাইল commit হবে সেটা ঠিক করতে
git commit -m "msg"	Local checkpoint বানাতে	Criminal কোডের history save করতে
git push origin main	Local Criminal → Remote পাঠাতে	কোড টিম/সার্ভারের সাথে share করতে
git pull origin main	Remote Criminal → Local sync করতে	অন্যদের কোড লোকালে আপডেট পেতে
git branch	Criminal প্রোজেক্টের branch লিস্ট দেখতে	একসাথে একাধিক feature/manage করতে
git checkout -b new-branch	নতুন Criminal feature এর জন্য branch বানাতে	main code safe রেখে আলাদা feature বানাতে
git switch new-branch যদি আবার manually move করতে চান
git merge branchname	Branch এর কাজ Criminal main এ মেশাতে	feature শেষ হলে মূল কোডে যোগ করতে
git status	Criminal এ কোন ফাইল পরিবর্তিত হয়েছে দেখতে	কোন কাজ এখনো commit হয়নি বুঝতে
git log	Criminal এর সব commit history দেখতে	কে কখন কী করেছে track করতে
git stash	Criminal এ কাজ সাময়িক save করে অন্য কিছু করতে	কাজ অর্ধেক শেষ হলে temporary সেভ রাখতে

👉 এখন Criminal প্রোজেক্টের Local ↔ Remote flow একদম clear:

Local → Remote → add → commit → push

Remote → Local → clone / pull


🔹 Pull Request কী?

Pull Request (PR) হচ্ছে একটা অনুরোধ (Request) GitHub/GitLab/Bitbucket এ, যেখানে তুমি তোমার কোডের পরিবর্তন (changes/commits) অন্য কারও ব্রাঞ্চে merge করার অনুমতি চাইছো।

👉 সাধারণত এটা হয় যখন অনেকজন একসাথে কোনো প্রোজেক্টে কাজ করে।
👉 তুমি নতুন কোনো ফিচার/বাগ ফিক্স করে ফেলেছো, এখন সেটা main/master ব্রাঞ্চে আনতে চাইছো।

🔹 Pull Request এর ধাপ (Step by Step)

তুমি আলাদা একটা ব্রাঞ্চে কাজ করবে (যেমন feature-login)।

কাজ শেষ হলে তুমি main ব্রাঞ্চে merge করার জন্য Pull Request ওপেন করবে।

টিম মেম্বাররা রিভিউ করবে, কমেন্ট করবে, দরকার হলে আবার পরিবর্তন করবে।

রিভিউ শেষ হলে merge করা হবে।

🔹 Two-way Pull Request (2 দিকের Pull Request)

এটার মানে হতে পারে দুইভাবে PR করা যায় ⬇️

✅ ১) Fork থেকে Pull Request

তুমি কারও প্রোজেক্ট fork করলে,

নিজের fork করা repo তে কাজ করলে,

তারপর মূল repo তে (upstream repo) PR পাঠাবে।

👉 একে বলে External Contribution PR।

✅ ২) Same Repository এর ভেতরে Pull Request

একই repo এর মধ্যে তুমি feature-branch থেকে main এ PR করবে।

এটা সাধারণত টিম প্রোজেক্টে হয়।

👉 একে বলে Internal PR।

🔹 সংক্ষিপ্ত বাংলা উদাহরণ

ধরো তোমার টিমমেট Rahul main repo তে কাজ করছে।

তুমি feature-login ব্রাঞ্চে কাজ করছো।

👉 ২ভাবে PR হতে পারে:

Same repo PR → feature-login → merge → main (internal)

Forked repo PR → তোমার fork → merge → Rahul এর main repo (external)

🔹 Git এ File Tracking System
১) Untracked File

নতুন ফাইল বানালে Git জানে না সেটা ট্র্যাক করবে কিনা।

Example:

git status


➡️ দেখাবে Untracked files

👉 Solution → ট্র্যাক করতে চাইলে:

git add filename

২) Tracked File (Staged/Committed)

একবার git add দিলে Git ওই ফাইলকে track করবে।

এরপর যদি নতুন edit না থাকে → status এ clean দেখাবে।

৩) Modified File

ট্র্যাক করা ফাইল edit করলে সেটা Modified হবে।

git status


➡️ দেখাবে modified: filename

👉 আবার stage করতে চাইলে:

git add filename

🔹 Merge Conflict কবে হয়?

👉 ধরো main branch এ একটি লাইন আছে:

const name = "Rahim";


তুমি feature branch এ সেটাকে বদলালে:

const name = "Karim";


এবং টিমমেট main branch এ একই লাইন বদলালো:

const name = "Hasan";


➡️ এখন তুমি যখন feature কে main এ merge করবে → Git বুঝবে না কোনটা রাখবে।
এটাই merge conflict।

Conflict কিভাবে চেনা যাবে?
git merge feature


➡️ দেখাবে CONFLICT message।
➡️ Code ফাইলে conflict এর জায়গায় এমন লাইন আসবে:

<<<<<<< HEAD
const name = "Hasan";
=======
const name = "Karim";
>>>>>>> feature

Conflict Fix করার ধাপ

ফাইল খুলে ঠিক করো কোন কোড ঠিক।

const name = "Karim";   // final decision মানে feature branch-এর কোড সঠিক ✅

What to do now?

আগে নিশ্চিত হও তুমি feature/new-branch এ আছো:

git checkout feature/new-branch


main এর সাথে মর্জ করো (তাহলে Karim code main এ যাবে):

git checkout main
git merge feature/new-branch


এখন main branch এ code হবে:

const name = "Karim";   // final decision

Stage করো:

git add filename


Merge সম্পূর্ণ করো:

git commit

🔹 যদি Current Code ভুল হয় → কিভাবে Fix করবে?

শেষ commit undo করতে চাইলে:

git reset --soft HEAD~1


👉 Commit cancel হবে কিন্তু code safe থাকবে।

পুরোপুরি code ও commit ফেলে দিতে চাইলে:

git reset --hard HEAD~1


👉 এক ধাপ পিছনে চলে যাবে।

🔹 Commit History কিভাবে দেখবে?
git log


➡️ commit ID, author, date সব দেখাবে।

👉 শুধু short log দেখতে চাইলে:

git log --oneline
